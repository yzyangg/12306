server:
  port: 9000
spring:
  application:
    name: index12306-gateway${unique-name:}-service
  profiles:
    active: aggregation
    # active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.119.129:8848
